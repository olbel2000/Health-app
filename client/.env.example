# API Configuration
# For local development, leave empty (uses Vite proxy)
# For production, set your backend URL (e.g., Cloud Run, Railway, etc.)
VITE_API_URL=

# Set to 'true' to run in demo mode without backend
VITE_DEMO_MODE=false
